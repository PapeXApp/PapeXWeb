# Allow all crawlers
User-agent: *
Allow: /
# RDH receipt web fallback — per-session, expiring, no SEO value. Belt-and-
# suspenders alongside the route's own noindex meta tag (app/r/page.tsx).
Disallow: /r

# Sitemap location
Sitemap: https://papex.app/sitemap.xml 

# Crawl-delay for better server performance
Crawl-delay: 1

# Allow all major search engines
# Each named group below is its own record and otherwise overrides the
# default `*` group entirely for that crawler, so /r has to be re-disallowed
# in every one of them or these bots would crawl it.
User-agent: Googlebot
Allow: /
Disallow: /r

User-agent: Bingbot
Allow: /
Disallow: /r

User-agent: Slurp
Allow: /
Disallow: /r

User-agent: DuckDuckBot
Allow: /
Disallow: /r

# Block unnecessary crawlers
User-agent: AhrefsBot
Disallow: /

User-agent: MJ12bot
Disallow: / 