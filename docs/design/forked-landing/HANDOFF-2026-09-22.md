# HANDOFF — 2026-09-22 website smoothing + real-app phone mockups

Read `CLAUDE.md` (repo root) and `../../../CLAUDE.md` (workspace map) first.
Start any follow-up chat in `Documents/Claude` with `/goal <ask>` — the orchestrator picks this file up
from `code/.claude/plans/2026-09-22-website-smoothing-audit.md` (the full audit + execution plan + outcome).

## State
- Branch `feat/forked-landing-redesign-v2`, working tree clean. **6 local commits ahead of origin, NOT pushed:**
  `5655237` merge of Will's `design/interactive-prototype` (Sep 10) · `03582d1` smoothing + error sweep ·
  `8d94465` tighter rhythm · `ec8fbf4` app-media pipeline · `665d028` phone mockups rebuilt.
  Pushing to `main` deploys papex.app immediately (no CI) — ship via PR after Nico signs off.
- Dev server: `npm run dev -- -p 3001` (or the `papex-web` preview). Never `npm run build` while it runs.
- ffmpeg installed via Homebrew (for `npm run media:import`, see below).

## What changed today
1. **Rhythm.** One token `--section-pad-y: clamp(40px,4.5vw,60px)` on both path homes; customers' `--section-pad`
   aliases it. Heroes are content-height (no 100vh). How-it-works pin top-aligned, 55vh per step.
2. **Colour story** (3 flips per page by story beat): business navy·light·light·navy·navy·light·navy-footer;
   customers light·light·light·navy·light·navy·navy·navy-footer. **Footer now mounts inside FlowGround**
   (`<FlowSection ground="navy"><SiteFooter inFlow/></FlowSection>` in both `index.tsx`) and crossfades;
   `.scrim` removed. Legacy FramerPageShell routes keep the flat navy footer (no `rd-footer-flow` class).
3. **Type.** Every h1/h2/stat on the shared tokens (h1 caps 72/70, stat 72, eyebrow 12).
   **Tailwind gotcha: use `text-[length:var(--fs-*)]` — plain `text-[var()]` is read as a colour and silently
   drops the size to 16px.**
4. **Error sweep.** Dead Download CTAs → App Store link; "see documentation" → `/pci`; demo-form phone `tel:`;
   footer Platform links → /customers, /business, /support; social chips hidden until `SOCIAL_LINKS` in
   `components/brand/links.ts` is filled; sitemap + OG/twitter meta; h4→h3; sr-only h2 on Proof; "At a glance" =
   one compact strip.
5. **Copy (Nico's calls).** Nav + footer "For Businesses"; "No contract." removed from the business ribbon;
   fork business half = "Modernize your checkout. No cost. No hassle.", business hero = "Modern checkout. Zero paper."
   Customer side unchanged. Rails + ticker ribbons kept.
6. **Demo phones = hand-built mockups of the real app.** New kit `components/paths/customer/appui/`
   (Chrome/Screens/Clip/data). Hero = App Clip flow (lock-screen card → Reading your receipt → clip receipt →
   Save to PapeX; still decodes via `lib/escpos`). Features = real Receipts screen (search "blue") + USPS
   detail with share sheet. Walkthrough = card → clip receipt → Receipts tab.
   **Nico's rule: captures are REFERENCE ONLY, never embedded.** Reference frames: `app-media/reference/`.
   The `app-media/` import pipeline (`manifest.json`, `scripts/import-app-media.mjs`, `AppMedia.tsx`) is kept
   with every slot empty; "refresh the app media" = add frames to reference/, then update the mockups.

## Open items
- Footer social URLs (LinkedIn / X / Instagram) still empty → chips hidden.
- Hero/walkthrough merchant logos are monograms, not real marks; receipts group dates are invented.
- Nico's PapeXV2 dev build crashes on the Stores tab: `Property 'HEADER_BUBBLE_TOP' doesn't exist` (stores.tsx).
- Light→navy ink crossfade is visible for ~1 screen at the demo-form/footer edge (by design of `--flow-fg`).
- Footer now inside `<main>` on the path homes (landmark change) — a11y review if it matters.
- Not yet checked on a real device; `npm run build` not run today (tsc + lint clean).
